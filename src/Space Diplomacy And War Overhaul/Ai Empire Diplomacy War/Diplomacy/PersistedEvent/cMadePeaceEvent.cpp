#include "stdafx.h"
#include "cMadePeaceEvent.h"
#include <Spore-Mod-Utils/Include/SporeModUtils.h>

using namespace SporeModUtils;
using namespace Simulator;

/// AUTOGENERATED METHODS ///

size_t cMadePeaceEventFactory::GetTypes(uint32_t* pDstTypes, size_t count) const {
	if (count >= 1) {
		pDstTypes[0] = cMadePeaceEvent::NOUN_ID;
		return 1;
	}
	return 0;
}
Object* cMadePeaceEventFactory::Create(uint32_t type, ICoreAllocator* pAllocator) {
	return new cMadePeaceEvent();
}
const char* cMadePeaceEventFactory::GetName(uint32_t type) const {
	return "cMadePeaceEvent";
}

uint32_t cMadePeaceEvent::GetCastID() const {
	return TYPE;
}
uint32_t cMadePeaceEvent::GetNounID() const {
	return NOUN_ID;
}

void* cMadePeaceEvent::Cast(uint32_t type) const {
	CLASS_CAST(cMadePeaceEvent);
	CLASS_CAST(cPersistedDiplomacyEvent);
	CLASS_CAST(cPersistedEvent);
	PARENT_CAST(cGameData);
	return nullptr;
}

bool cMadePeaceEvent::Write(Simulator::ISerializerStream* stream)
{
	return cPersistedDiplomacyEvent::Write(stream) && Simulator::ClassSerializer(this, ATTRIBUTES).Write(stream);
}
bool cMadePeaceEvent::Read(Simulator::ISerializerStream* stream)
{
	return cPersistedDiplomacyEvent::Read(stream) && Simulator::ClassSerializer(this, ATTRIBUTES).Read(stream);
}

bool cMadePeaceEvent::Active(){
	return cPersistedDiplomacyEvent::Active() && !DiplomacyUtils::War(empire1.get(), empire2.get());
}

/// END OF AUTOGENERATED METHODS ///
////////////////////////////////////

Simulator::Attribute cMadePeaceEvent::ATTRIBUTES[] = {
	// Add more attributes here
	// This one must always be at the end
	Simulator::Attribute()
};